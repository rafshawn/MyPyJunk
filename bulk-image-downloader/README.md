# Bulk Image Downloader
```py img.py```

### Install Dependencies:
- `pip install requests`
- `pip install bs4`
- `pip install wget`
- `pip install urljoin`

## TODO:
- [x] Fetch from URL and parse
- [x] Save downloaded images to a "downloads" folder
- [x] Ignore exceptions and move on to next download
- [ ] Output all images found to an `out.txt` file
- [ ] Let user define input
  - [ ] Either as a URL or as a `in.txt` file containing multiple URLs
- [ ] Create GUI
- [ ] Create folder marked with date and time for every operation inside downloads folder
- [ ] Refine GUI